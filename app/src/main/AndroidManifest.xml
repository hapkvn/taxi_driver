<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Game"
        android:usesCleartextTraffic="true"
        tools:ignore="ExtraText">
        <activity
            android:name=".ListView.users_list"
            android:exported="false" />
        <activity
            android:name=".admin.adminMemu"
            android:exported="false" />
        <activity android:name=".MainMenu.settingLayout"
            android:allowEmbedded="false" />
        <activity
            android:name=".MainMenu.menuLayout"
            android:exported="false" />
        <activity
            android:name=".admin.admin_manage_layout"
            android:exported="false" />
        <activity
            android:name=".MainScene"
            android:exported="false" />
        <activity
            android:name=".login.loginLayout"
            android:exported="false" />
        <activity
            android:name=".login.resignLayout"
            android:exported="false" />
        <activity
            android:name=".login.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>